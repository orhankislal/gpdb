platform: linux
image_resource:
  type: docker-image
inputs:
  - name: gpdb_src
  # - name: bin_gpdb
  - name: postgis_src
outputs:
params:
  TEST_OS: ""
run:
  path: gpdb_src/concourse/scripts/postgis_check_gpdb2.bash
